# Rust_Shell
This Repo contains a program that simulates a shell cli in Rust. Current this shell has the following commands
- echo
- exit
- type

In addition this shell can execute external programs

To compile and run this program type the command "./your_program.sh"
